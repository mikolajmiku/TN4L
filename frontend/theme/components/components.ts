import { CustomButton } from "./button";
import { CustomDrawer as CustomDrawerStyle } from "./drawer";
import { CustomHeadingStyle } from "./heading";
import { CustomText as CustomTextStyle } from "./text";

export const components = {
  Text: CustomTextStyle,
  Drawer: CustomDrawerStyle,
  Heading: CustomHeadingStyle,
  Button: CustomButton,
};
